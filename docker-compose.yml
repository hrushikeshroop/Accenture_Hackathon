version: '3.8'

services:
  controlplane:
    build: .
    container_name: controlplane_demo
    ports:
      - "8000:8000"
      - "8501:8501"
    environment:
      # Passes the Groq API key from your local .env file into the container
      - GROQ_API_KEY=${GROQ_API_KEY}
